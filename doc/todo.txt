1. list of default parameters for XSEC calculation: GC_UNI (must)

2. modify getSimComplStatus(fullFileName) as separate function

3. remove end -s from functions (done)

4. add unique requirement for the list of saveResPar (done)

5. check if cell exist saveResPar (done)

6. address setSeed issue

7. checkCalcStatus function, put it separately

8. check what means isLog ? in data_for_fuelpin.m

9. simStatus, add 0,1,2
